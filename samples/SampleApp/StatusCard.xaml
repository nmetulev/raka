<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="SampleApp.StatusCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="8" Padding="16">
        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <FontIcon x:Name="CardIcon" Glyph="&#xE946;" FontSize="24" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="TitleText" Text="Title" FontWeight="SemiBold" FontSize="16" />
                <TextBlock x:Name="StatusText" Text="Status" Opacity="0.7" FontSize="13" />
            </StackPanel>
            <Ellipse Grid.Column="2" Width="12" Height="12" VerticalAlignment="Center">
                <Ellipse.Fill>
                    <SolidColorBrush x:Name="StatusBrush" Color="#4CAF50" />
                </Ellipse.Fill>
            </Ellipse>
        </Grid>
    </Border>
</UserControl>
