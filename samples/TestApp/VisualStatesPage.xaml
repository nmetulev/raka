<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="TestApp.VisualStatesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="24" MaxWidth="600">
            <TextBlock Text="Visual State Tests" Style="{StaticResource TitleTextBlockStyle}" />

            <!-- Button with obvious custom visual states for testing -->
            <StackPanel Spacing="8">
                <TextBlock Text="Button with obvious state colors" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <Button x:Name="StateButton" Content="Hover or Press Me" Width="200" Height="40">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#2D7D46" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="BorderBrush" Value="#1B5E30" />
                            <Setter Property="BorderThickness" Value="2" />
                            <Setter Property="HorizontalAlignment" Value="Left" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Grid x:Name="RootGrid" Background="{TemplateBinding Background}"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              BorderThickness="{TemplateBinding BorderThickness}"
                                              CornerRadius="4" Padding="12,8">
                                            <VisualStateManager.VisualStateGroups>
                                                <VisualStateGroup x:Name="CommonStates">
                                                    <VisualState x:Name="Normal" />
                                                    <VisualState x:Name="PointerOver">
                                                        <Storyboard>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Background">
                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="#E65100" />
                                                            </ObjectAnimationUsingKeyFrames>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="BorderBrush">
                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="#BF360C" />
                                                            </ObjectAnimationUsingKeyFrames>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="White" />
                                                            </ObjectAnimationUsingKeyFrames>
                                                        </Storyboard>
                                                    </VisualState>
                                                    <VisualState x:Name="Pressed">
                                                        <Storyboard>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Background">
                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="#B71C1C" />
                                                            </ObjectAnimationUsingKeyFrames>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="BorderBrush">
                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="#7F0000" />
                                                            </ObjectAnimationUsingKeyFrames>
                                                        </Storyboard>
                                                    </VisualState>
                                                    <VisualState x:Name="Disabled">
                                                        <Storyboard>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Background">
                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="#9E9E9E" />
                                                            </ObjectAnimationUsingKeyFrames>
                                                        </Storyboard>
                                                    </VisualState>
                                                </VisualStateGroup>
                                            </VisualStateManager.VisualStateGroups>
                                            <ContentPresenter x:Name="ContentPresenter"
                                                Content="{TemplateBinding Content}"
                                                Foreground="{TemplateBinding Foreground}"
                                                HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                <TextBlock Text="Normal=Green, PointerOver=Orange, Pressed=Red" Foreground="{ThemeResource SystemFillColorNeutralBrush}" />
            </StackPanel>

            <!-- Toggle Controls -->
            <StackPanel Spacing="8">
                <TextBlock Text="Toggle Controls" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <CheckBox x:Name="TestCheckBox" Content="Check me" />
                <RadioButton x:Name="RadioA" Content="Option A" GroupName="TestGroup" IsChecked="True" />
                <RadioButton x:Name="RadioB" Content="Option B" GroupName="TestGroup" />
                <RadioButton x:Name="RadioC" Content="Option C" GroupName="TestGroup" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
